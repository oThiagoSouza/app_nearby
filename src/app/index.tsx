import { Text, View } from "react-native"

export default function Index() {
    return (
        <View style={{ flex: 1, marginTop: 50,
            alignItems:"center"
        }}>
            <Text style ={{ fontSize: 36, color: '#fff'}}>Hello React Native</Text>
        </View>
            

    )
}